using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GenerateMysteryObject : MonoBehaviour
{
    //public GameObject Chest;
    //public GameObject[] MysteryObject;
    
    void Start()
    {
        //int randomNumber = Random.Range(0, MysteryObject.Length);
        //Instantiate(MysteryObject[randomNumber], Chest.transform.position, Chest.transform.rotation);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
